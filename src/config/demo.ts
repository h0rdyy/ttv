/**
 * The shared demo campaign lives on the technical demo account (see
 * supabase/migrations/0030). Anonymous demo links are served through its
 * session; signed-in users join it as GMs via join_demo_campaign().
 */
export const DEMO_CAMPAIGN_NAME = 'Демо: таверна у дороги';
